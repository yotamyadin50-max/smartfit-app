# סקירת מנהל — SmartFit / Ascend AI

**תאריך:** 19.07.2026
**מה נעשה:** סבב עבודה מלא על האפליקציה — שלושה סוכני בדיקה עברו עליה במקביל (תקינות קוד, אבטחה, חיזוק למובייל), אחריהם תיקון בפועל של כל מה שנמצא, סבב אימות שני שתפס עוד באג אמיתי, ובניית APK להתקנה על הטלפון.

---

## 1. מה תוקן היום

### קריטי
- **AI chat לא עבד בכלל על ה-APK המותקן.** `VITE_API_BASE_URL` היה ריק, אז כל בקשת AI על הטלפון נפלה אוטומטית ל"מצב מקומי" (תשובות מוכנות מראש) בלי לגעת בשרת בכלל. הקמתי שרת זמני ברשת הביתית (`server/lanDevServer.js`, פורט 8787) ובניתי את ה-APK מול הכתובת שלו. **מגבלה:** זה עובד רק כשהטלפון על אותו Wi-Fi כמו המחשב. לפתרון קבוע צריך deploy אמיתי (Vercel כבר מוגדר בפרויקט — `vercel.json` קיים) — זו החלטה שמצריכה את האישור שלך כי זה חשיפה לאינטרנט הציבורי.
- **חישוב הקלוריות/מאקרו בארוחות שנוצרות** ([NutritionPage.tsx](src/pages/NutritionPage.tsx)) היה נוסחה קבועה בלי קשר למצרכים בפועל. עכשיו נשלחת בקשה ל-AI לקבל מספרים אמיתיים לפי המצרכים, עם הנוסחה כ-fallback אם ה-AI לא עונה בפורמט תקין. **הערה כנה:** בדקתי את זה בפועל מול השרת החי — המודל החינמי (Nvidia, כי למפתח אין כרגע קרדיט על gemini) לפעמים מתעלם מהמצרכים האמיתיים ומחזיר דוגמה גנרית. זו מגבלת איכות של המודל החינמי, לא באג בקוד — הקוד מזהה תשובה לא תקינה ונופל בחזרה לנוסחה בבטחה.
- **ה-prompt של הצ'אט** נשלח כמחרוזת טקסט שטוחה אחת במקום כ-messages array מובנה (system + היסטוריית שיחה אמיתית) — עכשיו זה מובנה נכון, מה שאמור לשפר משמעותית את איכות התשובות בשיחות ארוכות.
- **RLS policies פתוחים מדי** ב-`supabase/schema.sql` על טבלאות ה-social feed — כל משתמש מחובר יכול היה לערוך/למחוק פוסטים, תגובות ותגובות-לב של משתמשים אחרים. תיקנתי את הקובץ ל-policies מדויקים לפי בעלות/משתתפים. **חשוב: זה תיקון בקובץ בלבד — אני לא יכול ולא ניגשתי למסד הנתונים החי ב-Supabase. תיכנס ל-Supabase Studio → Authentication → Policies ותוודא איזה גרסה פעילה בפועל, ותריץ מחדש אם צריך.**
- **אין הגנת rate-limit על `/api/ai` בפרודקשן** (הייתה רק בשרת הפיתוח המקומי) — כל אחד יכול היה לשגר בקשות בלי הגבלה, מה שעולה כסף אמיתי. תוקן.

### חשוב
- **התראת "המנוחה נגמרה" באימון** ([WorkoutPage.tsx](src/pages/WorkoutPage.tsx)) לא הייתה עובדת בכלל ב-APK כי היא השתמשה ב-Web Notification API שלא נתמך ב-WebView של Android. הוחלף למנגנון ה-Capacitor הנכון שכבר קיים במערכת התזכורות.
- הוסרה הרשאת מיקום-רקע מיותרת (`ACCESS_BACKGROUND_LOCATION`) שלא הייתה עושה כלום בפועל, רק מציגה פרומפט הרשאה מפחיד יותר מהצורך.
- מסך הפתיחה (splash) לא היה נסגר לפי טעינה אמיתית, רק טיימר עיוור של 2 שניות — עכשיו נסגר לפי מצב האפליקציה בפועל.
- מספר גרסה (`versionCode`/`versionName`) לא היה מסונכרן בין קבצים — תוקן, כולל bump ל-versionCode שנדרש אם אי פעם תעלה גרסה ל-Play Store.
- כפתור מעבר מיקום אימון (בית/חדר כושר) ב-Training Plan היה קטן מדי למגע נוח (36px) — הוגדל ל-44px.
- נוקו שני קבצים גדולים ולא בשימוש מה-repo (`tiger.png`, `smartfit-logo.html`, יחד כ-5MB) וקובץ JS מת (`floatingAiChat.js`).
- עודכן חבילת react-router-dom לגרסה שמתקנת חולשת אבטחה (open redirect).

### מה שנתפס בסבב האימות השני (ולא הייתי מגלה בלי סבב שני)
- ה-messages array החדש בצ'אט לא היה מוגבל באורך, ובשיחה ארוכה יכול היה לחרוג מהמגבלה בשרת ולגרום להודעה מוזרה באנגלית לחזור כאילו היא תשובת AI אמיתית. תוקן עם הגבלת אורך + fallback מכובד יותר.
- הגנת ה-rate-limit הסתמכה על header שאפשר לזייף (`X-Forwarded-For` הכתובת הראשונה ברשימה) — תוקן לקחת את הכתובת האחרונה (זו שהשרת עצמו מוסיף ואי אפשר לזייף).

---

## 2. מה בכוונה לא תוקן, ולמה

- **חיבור Bluetooth לדופק לב** ([heartRate.ts](src/lib/heartRate.ts)) לא יעבוד בכלל ב-APK — הקוד משתמש ב-Web Bluetooth API שלא קיים ב-WebView של Android, למרות שההרשאות מוגדרות נכון ב-manifest. זה דורש plugin native (`@capacitor-community/bluetooth-le`) ושכתוב אמיתי של הפיצ'ר, ובעיקר — בדיקה על מכשיר Bluetooth אמיתי שאין לי גישה אליו. לא נכון לתקן "בעיוור" בלי אפשרות לבדוק.
- **המרת מודל ה-AI מ-nvidia (חינמי) ל-gemini (בתשלום)** — המודל בתשלום נכשל בכל בקשה כי אין קרדיט על המפתח הנוכחי. שיניתי את סדר הניסיון כך שהמודל בתשלום לא "עוצר" כל בקשה בכישלון מיותר, אבל אם תרצה איכות תשובות גבוהה יותר, צריך להוסיף קרדיט ב-OpenRouter.

---

## 3. רעיונות והצעות לשיפור (המנהל)

לא רק תיקוני באגים — כמה כיוונים אמיתיים שיכולים לחזק את האפליקציה:

1. **Deploy אמיתי ל-Vercel.** `vercel.json` כבר קיים ומוכן. זה הפתרון הקבוע ל-AI chat על הטלפון (בלי תלות באותו Wi-Fi). דורש שתתחבר בעצמך ל-Vercel (אני לא יכול להתחבר בשמך) — תגיד לי אם תרצה שאדריך אותך בתהליך.
2. **מדד "confidence" למאקרו של ה-AI.** במקום סימון בינארי (הערכה / לא הערכה), אפשר להראות למשתמש עד כמה ה-AI "בטוח" — למשל אם התשובה לא הזכירה את שם אחד המצרכים בתיאור, סמן את זה כחשוד גם אם ה-JSON תקני טכנית.
3. **מעקב שימוש ב-AI.** אין כרגע שום דרך לדעת כמה בקשות AI נשלחות ביום, כמה עולות כסף בפועל, או כמה נופלות ל-local mode. לוג פשוט (אפילו קובץ מקומי) יעזור להבין את העלות האמיתית לפני שמוסיפים משתמשים.
4. **Onboarding למכשיר Bluetooth.** כשתחליט לתקן את חיבור דופק הלב, כדאי מסך "בדוק שה-Bluetooth דלוק ושהמכשיר בטווח" לפני הניסיון להתחבר — משתמשים לא מבינים למה חיבור נכשל בלי הקשר.
5. **מסך "מה חדש" בכל עדכון גרסה.** יש כבר מנגנון updater (`src/lib/updater.ts`) שמזהה גרסה חדשה — אפשר לצרף לו מסך קצר עם 2-3 שורות "מה השתנה" במקום רק להוריד עדכון בשקט.
6. **גיבוי לפני מחיקת קבצי CSS מתים.** יש עוד כ-30 שורות CSS ב-`index.css` ששייכות ל-`floatingAiChat.js` שמחקתי — לא נגעתי בהן כי זה ניקוי קוסמטי בלבד וללא סיכון תפקודי, אבל שווה מעבר נפרד כשיהיה זמן.

---

## 4. איך להתקין ולהריץ

1. פתח את קובץ ה-APK שנשלח לך בצ'אט, ישירות בטלפון.
2. אם Android חוסם התקנה — אשר "התקן ממקורות לא ידועים" (זו אפליקציית פיתוח, לא מה-Play Store, אז זו התראה תקינה וצפויה).
3. **חשוב:** כדי שה-AI יעבוד, הטלפון חייב להיות על אותו Wi-Fi כמו המחשב הזה, כי השרת הזמני (`npm run dev:lan-api`) רץ מקומית על הכתובת `192.168.1.97:8787`. הרצתי אותו כרגע ברקע — אם תסגור את המחשב או תנתק אותו מהרשת, ה-AI ייפול בחזרה ל"מצב מקומי" (עדיין עובד, פחות חכם).
4. לבנייה חוזרת בעתיד: `npm run dev:lan-api` (בטרמינל אחד) ואז `VITE_API_BASE_URL=http://<IP-של-המחשב>:8787 npm run build:android` ואז `cd android && ./gradlew assembleDebug` (עם JDK תואם ל-Gradle — ב-Android Studio זה `Program Files\Android\Android Studio\jbr`, לא ה-JDK הרגיל של המערכת שגרם לכשל בבנייה הראשונה כי הוא גרסה חדשה מדי).
